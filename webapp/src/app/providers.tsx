// Deprecated: Unused wrapper. Theme is provided via ThemeProvider in layout.
import React from "react";
export function Providers({ children }: Readonly<{ children: React.ReactNode }>) {
  return <>{children}</>;
}
